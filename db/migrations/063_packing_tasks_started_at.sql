-- =============================================================================
-- BFC24 WMS v2 — Migration 063: started_at у wms.packing_tasks
-- =============================================================================
-- Нужно для диспетчерской (карточки "Отгрузки в работе"): хотим показывать
-- не только % выполнения сборки/упаковки, но и кто именно сейчас собирает/
-- упаковывает, сколько штук осталось, и сколько времени отгрузка уже в работе.
-- Время старта сборки уже есть (wms.pick_waves.accepted_at), а вот у упаковки
-- отдельного "начал работу" таймстампа не было вообще — updated_at затирается
-- повторно при завершении (status='done'), так что использовать его нельзя.
-- Добавляем чистую колонку, заполняется в packing.service.js в момент, когда
-- упаковщик берёт задачу в работу (packer_id=$1, status='in_progress').
-- =============================================================================

BEGIN;

ALTER TABLE wms.packing_tasks ADD COLUMN IF NOT EXISTS started_at TIMESTAMPTZ;

COMMIT;
