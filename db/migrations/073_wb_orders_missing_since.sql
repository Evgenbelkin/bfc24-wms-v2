-- =============================================================================
-- BFC24 WMS v2 — Migration 073: wb_orders.missing_since
-- =============================================================================
-- ФИКС 23.09.2026 (владелец, аккаунт "ИП Китай" сразу после обновления
-- токена: "Волна сформирована (41 заказ(ов) исключено)" - все эти заказы
-- помечены 'external' ("Забрано в ЛК WB"), хотя в кабинете WB они висели как
-- "Новые"). Причина: реконсилиация в wb.service.js::fetchAndUpsertOrders
-- метила заказ status='external' сразу по ОДНОМУ пропуску в ответе
-- /orders/new - если этот единственный ответ WB на каком-то тике оказался
-- неполным (транзиентный сбой, разминка только что обновлённого токена,
-- пагинация) - все "живые" заказы, которых не оказалось в этом одном
-- неполном ответе, немедленно и без права на ошибку улетали в 'external'.
--
-- missing_since - "с какого тика заказ впервые не пришёл в ответе WB".
-- Теперь в 'external' заказ уходит только если он отсутствовал ДВА раза
-- подряд (на разных вызовах синка) - один плохой ответ WB больше не может
-- обнулить целую волну.
-- =============================================================================

BEGIN;

ALTER TABLE wms.wb_orders ADD COLUMN missing_since TIMESTAMPTZ;

COMMIT;
