-- =============================================================================
-- BFC24 WMS v2 — Migration 030: данные о доставке заявки (водитель/машина)
-- =============================================================================
-- Склад вносит ФИО водителя и марку машины при физическом приезде поставки -
-- сверяет, кто привёз, до начала приёмки. Заполняется складом (не клиентом),
-- поэтому просто текстовые поля на самой заявке, без отдельной таблицы.
-- =============================================================================

BEGIN;

ALTER TABLE wms.inbound_orders
  ADD COLUMN driver_name  TEXT,
  ADD COLUMN vehicle_make TEXT;

COMMIT;
