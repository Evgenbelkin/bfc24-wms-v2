-- =============================================================================
-- BFC24 WMS v2 — Migration 037: порог подсорта — со склада на товар
-- =============================================================================
-- Миграция 036 положила reorder_min_qty/reorder_min_days на
-- wms.wb_seller_warehouses (один порог на пару склад+товар не задавался бы —
-- по факту порог был ОБЩИЙ для склада, единый для всех товаров на нём).
-- По факту у разных товаров разная оборачиваемость, порог нужен на товар,
-- одинаковый на всех его складах — переносим колонки на wms.items и убираем
-- со складов (продукт ещё не был в проде, переносить данные не с чего).
-- =============================================================================

BEGIN;

ALTER TABLE wms.items
  ADD COLUMN reorder_min_qty  INT,
  ADD COLUMN reorder_min_days NUMERIC(6,1);

ALTER TABLE wms.wb_seller_warehouses
  DROP COLUMN reorder_min_qty,
  DROP COLUMN reorder_min_days;

COMMIT;
