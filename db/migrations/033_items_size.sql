-- =============================================================================
-- BFC24 WMS v2 — Migration 033: техразмер товара
-- =============================================================================
-- Для одежды/обуви полезно видеть размер прямо в составе отгрузки, наравне
-- с артикулом продавца и артикулом WB. Подтягивается автоматически при
-- синхронизации карточек из WB (techSize/wbSize), либо проставляется
-- вручную в справочнике товаров.
-- =============================================================================

BEGIN;

ALTER TABLE wms.items
  ADD COLUMN size TEXT;

COMMIT;
